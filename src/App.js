import './App.css';
import Home from './components/Home';


function App() {

  

  // let dataItems = [];
  // for(let i=0; i<Data.length; i++){
  //   dataItems.push(<Home idNum={Data[i].id} titleText={Data[i].title} infoText={Data[i].info}/>)
  // }

  return (
    <div>
      <Home />
    </div>
  );
}

export default App;
